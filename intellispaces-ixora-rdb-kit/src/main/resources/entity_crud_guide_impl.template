package {{$generatedArtifactPackageName}};

{{for importedClass : $importedClasses}}
import {{$importedClass}};
{{end}}

/**
 * Autogenerated implementation of the CRUD guide for entities of the domain {@link {{$sourceArtifactName}}}.<p/>
 *
 * This class was generated automatically. Don't try to change it.
 */
@Guide
{{$generatedAnnotation}}
public class {{$generatedArtifactSimpleName}} implements {{$guideType}} {

{{when $entityHasIdentifier}}
  @Override
  public {{$entityHandleSimpleName}} getById(Transaction tx, {{$identifierType}} id) {
    Map<String, Object> params = MapsCustomizer.of(String.class, Object.class, "id", id);
    return tx.fetchData(Types.get({{$entityHandleSimpleName}}.class), "SELECT * FROM {{$entityTable}} {{$entityAlias}} WHERE {{$entityAlias}}.{{$identifierColumn}} = :id", params);
  }
{{end}}

  @Override
  public {{$entityHandleSimpleName}} create(Transaction tx, {{$entityHandleSimpleName}} contract) {
    throw new RuntimeException("Not implemented");
  }
}
